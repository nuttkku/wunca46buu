# LibreNMS Docker Compose Environment Variables
# =============================================
# Copy this file to .env and change all passwords/secrets
# Command: cp .env.example .env
#
# ⚠️ WARNING: This configuration is for WORKSHOP/LEARNING purposes only
# DO NOT use these default values in production environments!

# ==========================================
# MariaDB Database Configuration
# ==========================================
MYSQL_ROOT_PASSWORD=changeme_root_password_here
MYSQL_DATABASE=librenms
MYSQL_USER=librenms
MYSQL_PASSWORD=changeme_secure_password_here

# ==========================================
# LibreNMS Configuration
# ==========================================
# Application Key (run: openssl rand -base64 32)
APP_KEY=changeme_generate_random_32_char_key

# Database Connection
DB_HOST=db
DB_NAME=librenms
DB_USER=librenms
DB_PASSWORD=changeme_secure_password_here
DB_TIMEOUT=60

# Cache & Session Configuration
CACHE_DRIVER=redis
SESSION_DRIVER=redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=changeme_redis_password_here

# SNMP Configuration
# ⚠️ Change from 'public' to a secure community string
LIBRENMS_SNMP_COMMUNITY=changeme_snmp_community

# User/Group IDs for file permissions
PUID=1000
PGID=1000

# Timezone
TZ=Asia/Bangkok

# Optional Features
LIBRENMS_WEATHERMAP=false

# ==========================================
# Network Configuration
# ==========================================
# Port mappings (change if ports are already in use)
LIBRENMS_PORT=8000

# ==========================================
# Security Notes
# ==========================================
# 1. Change ALL passwords above to strong, unique values
# 2. Use: openssl rand -base64 16  to generate passwords
# 3. Use: openssl rand -base64 32  to generate APP_KEY
# 4. Change SNMP community from 'public' to something unique
# 5. Consider limiting port binding to localhost:
#    Example: LIBRENMS_PORT=127.0.0.1:8000
# 6. Enable Redis password and update REDIS_PASSWORD